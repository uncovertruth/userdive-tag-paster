info
  table(border="1" cellspacing="0")
    tr
      td pageId
      td visitorType
    tr
      td {pageId}
      td {visitorType}
  script.
    this.pageId = opts.pageid
    this.visitorType = opts.visitortype
