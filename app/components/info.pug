info
  table(border="1")
    tr(each="{name, value in data}")
      th {name}
      th {value}
  script.
    this.data  = JSON.parse(
      document.getElementsByTagName("info")[0].getAttribute("status")
      )
