'use strict';

(function (root, document, chrome) {
  // chrome.extension.onRequest.addListener((request, sender, sendResponse) => {
  // chrome.runtime.sendMessage({badge: 'update'}, (response) => {
  //   alert(response);
  //   chrome.browserAction.setBadgeText({'text': response || 'fail'});
  // });
  // });
})(window, document, chrome);
